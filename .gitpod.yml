image:
  file: .gitpod.Dockerfile

tasks:
  - init: cd /workspace/go-echo-demo/server &&  go get -u
    command: cd /workspace/go-echo-demo/server &&  go run ./main.go
ports:
  - port: 8080
    onOpen: open-preview
